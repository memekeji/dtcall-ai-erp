# DT企业智能管理系统 AI 功能分析与落地规划

> 文档位置：`documents/06_AI功能分析与落地规划.md`
> 适用对象：产品负责人、技术负责人、后端开发、前端开发、测试、实施交付、AI 功能负责人
> 文档目标：基于当前代码和业务文档，分析项目已有 AI 能力与业务入口，规划一组实用、可落地、可分阶段建设的 AI 功能。

---

## 1. 结论摘要

DT 企业智能管理系统已经具备较完整的 AI 能力雏形：模型配置、AI 聊天、知识库、RAG、意图识别、工作流、工作流执行记录、审计日志、效果反馈、A/B 测试、页面 AI 助手以及多个业务模块中的 AI 分析接口。

因此，下一阶段不建议继续单纯新增孤立 AI 页面，而应优先建设“业务页面上下文助手 + 统一 AI 服务治理 + 可复用工作流模板”的体系。

推荐第一优先级：

1. 审批与财务 AI 审核助手。
2. 合同 AI 审查助手。
3. 客户跟进与销售助手。
4. 项目风险与周报助手。
5. 库存与生产预警助手。
6. 企业知识库问答助手。

推荐第一阶段目标不是让 AI 自动替人做决定，而是让 AI 做“总结、识别风险、生成建议、预填内容、整理待办”，所有高风险动作仍由用户确认。

---

## 2. 当前项目理解

### 2.1 技术结构

当前项目是基于 Django 5.2 的企业管理平台，采用多应用模块化结构：

| 层级 | 当前实现 |
|---|---|
| 后端框架 | Django 5.2.1 |
| API 能力 | Django REST Framework、SimpleJWT、Session |
| 前端形态 | Django Templates + LayUI + jQuery + 静态 JS/CSS |
| 核心业务 | CRM、合同、财务、项目、任务、审批、消息、OA、个人办公、网盘、生产、库存 |
| AI 中台 | `apps.ai`、`apps.ai_orchestrator` |
| 文件与知识 | 企业网盘、Office/PDF 文档解析依赖、AI 知识库、向量表 |
| 部署依赖 | Docker、docker-compose、PostgreSQL、Redis、Gunicorn、AI Orchestrator Sidecar |

### 2.2 业务闭环

项目业务可以抽象为四条主线：

| 主线 | 业务链路 | AI 价值点 |
|---|---|---|
| 经营闭环 | 客户线索 → 客户 → 跟进 → 订单/合同 → 项目 → 财务 | 线索识别、客户画像、合同风险、回款预测、经营问答 |
| 生产库存闭环 | 产品 → BOM → 工艺 → 生产计划 → 任务 → 质检 → 入库 → 库存 | 排程建议、缺料预警、质量异常、库存预测、滞销分析 |
| 协同办公闭环 | 审批 → 消息 → 会议 → 日程 → 任务 → 个人办公 | 审批摘要、风险评估、会议纪要、待办提取、消息摘要 |
| 知识资产闭环 | 网盘/文档 → 知识库 → 向量 → RAG 问答 → 业务助手 | 制度问答、SOP 检索、项目文档问答、合同/文件摘要 |

---

## 3. 已有 AI 能力盘点

### 3.1 AI 中台能力

| 能力 | 现有位置 | 当前价值 |
|---|---|---|
| 模型配置 | `apps/ai/models.py`、`apps/ai/views.py` | 支持多供应商模型配置、启用状态、默认模型 |
| 加密 API Key | `EncryptedAPIKeyField` | 避免明文保存模型密钥 |
| AI 聊天 | `AIChat`、`AIChatMessage`、`templates/ai/chat.html` | 提供用户与 AI 的通用对话入口 |
| 运行时结构化数据 | `AIChatMessage.runtime_payload` | 支撑聊天结果携带任务、选项、确认动作 |
| 知识库 | `AIKnowledgeBase`、`AIKnowledgeItem`、`AIKnowledgeVector` | 支撑企业文档问答和 RAG |
| RAG 服务 | `apps/ai/services/rag_service.py` | 已有查询改写、混合检索、重排序思路 |
| 意图识别 | `ai_intent_classifier`、`enhanced_intent_service` | 将用户自然语言映射为数据查询、界面操作、知识库等意图 |
| 数据查询助手 | `query_service`、`intelligent_assistant` | 支持客户、订单、合同、项目、财务、生产、库存等查询 |
| AI 工作流 | `AIWorkflow`、`WorkflowNode`、`WorkflowConnection` | 支持模型、分类、提取、知识检索、HTTP、工具调用等节点 |
| 工作流执行 | `AIWorkflowExecution`、`NodeExecution` | 保存运行状态、输入输出、错误信息 |
| 人工交互 | `models_workflow_interaction.py` | 支持审批确认、输入、选择、反馈、检查点恢复 |
| 权限与审计 | `models_enhanced.py`、`workflow_permission_security.py` | 支持工作流权限、审计日志、API Key 管理 |
| 效果反馈 | `AIFeedback`、`AIBatchTest` | 支持 AI 结果评分和模型/提示词对比 |
| 前端页面助手 | `static/js/ai-agent-sdk.js` | 支持语音、页面总结、表单填充、文本润色、iframe 动作 |

### 3.2 业务模块 AI 入口

| 模块 | 已有入口 | 可扩展方向 |
|---|---|---|
| 客户 CRM | 客户 AI 机器人、客户意向、跟进数据 | 客户画像、下一步跟进建议、销售话术、线索评分 |
| 合同 | 合同风险分析、条款提取 | 合同摘要、付款节点、违约风险、履约提醒 |
| 财务 | 报销审核、异常检测 | 发票识别、回款风险、现金流摘要、凭证摘要 |
| 审批 | 审批风险评估 | 审批摘要、相似历史、建议意见、异常审批提醒 |
| 项目 | 项目风险预测、进度分析 | 项目周报、延期原因、资源冲突、交付风险 |
| 生产 | 生产计划优化建议 | 缺料预测、设备异常、质量异常、排产冲突 |
| 库存 | 库存需求预测 | 补货建议、滞销分析、呆滞料预警、安全库存建议 |
| 任务 | 任务工时预估与分配建议 | 自动拆任务、优先级建议、延期预警 |
| 消息 | 消息摘要、建议回复 | 通知聚合、待办提取、消息优先级 |
| OA/个人办公 | 会议纪要、任务项提取 | 自动生成待办、会议决议追踪、周报草稿 |
| 企业网盘 | 文件摘要、标签、文本提取 | 文档问答、文档归档建议、相似文件查找 |

---

## 4. 当前问题与机会

### 4.1 主要问题

| 问题 | 现象 | 影响 |
|---|---|---|
| AI 能力点状分散 | 各业务模块直接调用 `analysis_tools` 或 `AIClient` | 模型选择、日志、错误处理、成本统计难统一 |
| 多套模型调用层并存 | `AIClient`、`enhanced_model_service`、`AIAnalysisService`、业务分析工具并存 | 后续维护成本上升 |
| 业务 AI 结果缺少统一结构 | 有些接口返回自然语言，有些返回 dict，有些靠字符串解析 | 前端难复用，测试难覆盖 |
| 权限治理需要贯穿所有 AI 能力 | 查询服务已有权限映射，但业务 AI API 权限校验粒度不完全一致 | 可能产生越权分析或跨部门数据泄露 |
| 高风险动作需要确认机制 | 意图服务已经限制 create/update/delete，但业务场景仍需统一交互规范 | 避免 AI 直接修改关键业务数据 |
| 本地运行环境不完整 | 当前环境 `python` 不可用，Codex 自带 Python 未安装 Django | 影响测试、验证和持续交付 |

### 4.2 主要机会

| 机会 | 原因 |
|---|---|
| 前端统一助手入口已经存在 | `ai-agent-sdk.js` 可作为跨页面 AI 能力入口 |
| 业务数据模型足够丰富 | CRM、合同、财务、项目、生产、库存均已有结构化数据 |
| AI 工作流基础较完整 | 节点、连接、执行记录、权限、审计、交互均有雏形 |
| 知识库/RAG 已具备基础 | 可以将网盘、制度、SOP、项目文档转为问答能力 |
| 审批和消息是横向能力 | AI 结果可以天然转化为审批建议、消息提醒和待办 |

---

## 5. 三种落地路线

### 5.1 路线 A：继续按模块增加 AI 功能

做法：每个业务模块单独新增 AI 按钮、接口、提示词和返回格式。

优点：

- 开发快，局部改动小。
- 适合快速演示单点功能。

缺点：

- 容易形成新孤岛。
- 模型调用、日志、成本、权限难统一。
- 后续维护压力大。

适用场景：临时演示或某个模块极度紧急。

### 5.2 路线 B：先建设 AI 中台治理，再逐步接业务

做法：先统一 AI 调用层、日志、权限、反馈、结构化输出，再接入业务模块。

优点：

- 架构稳，后续扩展成本低。
- 有利于审计、安全、成本控制。

缺点：

- 第一阶段业务可见价值慢。
- 容易陷入平台建设过度。

适用场景：团队资源充足、希望长期产品化。

### 5.3 路线 C：业务助手先行，平台治理同步收敛

做法：优先选择 3-5 个高频业务场景做上下文助手，同时抽出统一 AI 调用、日志、权限、反馈规范。

优点：

- 业务可见价值快。
- 不牺牲长期架构。
- 可以边上线边沉淀通用能力。

缺点：

- 需要产品和技术负责人控制边界。
- 每个 AI 功能必须遵守统一输出和权限规范。

推荐：选择路线 C。

---

## 6. 推荐 AI 功能清单

### 6.1 P0：应优先落地

#### 6.1.1 审批与财务 AI 审核助手

目标用户：审批人、财务人员、管理者。

核心能力：

- 自动总结审批单/报销单关键信息。
- 标记异常金额、异常时间、缺少附件、重复报销、超预算风险。
- 展示相似历史审批或报销记录。
- 生成“同意/驳回/补充材料”的建议意见草稿。

落地方式：

- 复用 `apps/approval/ai_views.py`、`apps/finance/ai_views.py`。
- 统一输出结构：`risk_level`、`summary`、`risk_points`、`suggestion`、`recommended_action`、`confidence`。
- 前端接入审批详情页、报销详情页。

边界：

- AI 只给建议，不自动审批。
- 审批动作必须由人点击确认。

验收指标：

- 审批人能在详情页看到 AI 摘要和风险点。
- 高风险项必须展示原因。
- 用户可以复制或一键填入审批意见。

#### 6.1.2 合同 AI 审查助手

目标用户：销售、法务、合同管理员、管理者。

核心能力：

- 合同摘要。
- 关键条款提取：金额、付款节点、交付节点、违约责任、终止条件、有效期。
- 风险点识别：缺少付款条件、违约责任不清、交付时间不明确、金额异常。
- 履约提醒建议。

落地方式：

- 复用 `apps/contract/ai_views.py` 和合同模型已有 `ai_risk_level`、`ai_risk_points`、`ai_key_terms` 字段。
- 合同详情页增加 AI 审查面板。
- 结果保存到合同模型，避免重复调用。

边界：

- 不输出法律结论，只输出“业务风险提示”。
- 高风险结果需要用户确认后写入合同记录。

验收指标：

- 合同详情能一键生成摘要和风险点。
- 已生成结果能缓存复用。
- 修改合同后允许重新分析。

#### 6.1.3 客户跟进与销售助手

目标用户：销售、客户经理、销售主管。

核心能力：

- 客户画像：行业、等级、来源、历史跟进、订单/合同情况。
- 下一步跟进建议。
- 跟进记录润色。
- 根据通话/拜访记录提取客户意向。
- 生成销售话术或邮件/消息草稿。

落地方式：

- 复用 `CustomerAnalysisTool`、客户 AI 机器人、`ai-agent-sdk.js` 文本润色入口。
- 客户详情页和跟进记录表单接入上下文助手。

边界：

- 客户创建、修改仍走业务表单。
- AI 可以预填表单，但必须由用户保存。

验收指标：

- 销售在客户详情页能生成客户摘要。
- 跟进表单能一键润色或生成下一步计划。
- AI 结果不越权读取其他销售客户。

#### 6.1.4 企业知识库问答助手

目标用户：全员、实施、客服、行政、人事、生产人员。

核心能力：

- 查询制度、流程、SOP、操作手册、项目文档。
- 基于知识库给出答案和来源条目。
- 不知道时明确提示未找到，不编造。
- 与网盘文件摘要和标签联动。

落地方式：

- 复用 `AIKnowledgeBase`、`AIKnowledgeItem`、`AIKnowledgeVector`、`EnhancedRAGService`。
- 增强知识库条目的来源字段、权限过滤和引用展示。
- 在 AI 聊天中优先判断知识库意图。

边界：

- 必须按用户权限过滤知识库和网盘文件。
- 对答案附上来源，降低幻觉风险。

验收指标：

- 用户能问“报销流程怎么走”“某个 SOP 如何执行”。
- 返回答案包含来源标题。
- 无权限文档不会出现在答案中。

### 6.2 P1：第二阶段落地

#### 6.2.1 项目风险与周报助手

能力：

- 项目进度摘要。
- 延期任务识别。
- 工时异常分析。
- 自动生成项目周报草稿。
- 输出风险等级、责任人、建议动作。

复用入口：

- `apps/project/ai_views.py`。
- 项目、任务、工时、文档模型。

#### 6.2.2 库存与生产预警助手

能力：

- 库存需求预测。
- 安全库存建议。
- 缺料预警。
- 生产计划排程冲突提醒。
- 质量异常和设备异常总结。

复用入口：

- `apps/inventory/ai_views.py`。
- `apps/production/ai_views.py`。
- 生产计划、生产任务、BOM、库存流水。

#### 6.2.3 会议纪要到任务闭环

能力：

- 会议录音/内容生成纪要。
- 提取决议、待办、负责人、截止时间。
- 一键创建任务或个人待办。
- 后续提醒和完成追踪。

复用入口：

- `apps/oa/ai_views.py`。
- `apps/personal/views.py` 中已有会议纪要生成逻辑。
- `apps/task` 或 `apps/personal` 任务模型。

### 6.3 P2：第三阶段落地

#### 6.3.1 经营驾驶舱 AI 分析

能力：

- 对客户、合同、项目、财务、生产、库存做跨模块经营总结。
- 自动解释仪表盘波动。
- 生成管理层日报/周报/月报。

前置条件：

- 指标口径稳定。
- 跨模块数据权限明确。
- AI 调用日志和成本统计完善。

#### 6.3.2 AI 工作流模板市场

能力：

- 内置合同审查、报销审核、库存预警、项目周报、会议待办等模板。
- 支持管理员复制、编辑、发布、授权。
- 支持执行记录、失败重试、人工确认。

前置条件：

- 工作流权限、版本、审计、调试能力稳定。
- 节点配置 Schema 足够清晰。

#### 6.3.3 AI 质量评估与 A/B 测试

能力：

- 同一业务场景对比不同模型或提示词。
- 收集用户评分。
- 统计准确率、采纳率、平均耗时、调用成本。

复用入口：

- `AIFeedback`、`AIBatchTest`、`AIBatchTestVariant`、`AIBatchTestResult`。

---

## 7. 推荐架构原则

### 7.1 统一 AI 调用门面

建议新增或收敛为一个统一服务，例如：

```text
apps/ai/services/ai_gateway.py
```

职责：

- 根据任务类型选择模型配置。
- 封装 chat、embedding、summary、classification、extraction。
- 统一超时、重试、错误提示。
- 记录 `AILog`、token、耗时、模型、业务上下文。
- 提供结构化输出校验。

### 7.2 统一业务 AI 输出协议

建议所有业务 AI 接口都返回：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "summary": "",
    "risk_level": "low|medium|high|unknown",
    "risk_points": [],
    "suggestions": [],
    "recommended_action": "",
    "confidence": 0.0,
    "source_refs": [],
    "requires_confirmation": true
  }
}
```

### 7.3 所有高风险动作必须人工确认

高风险动作包括：

- 新增、修改、删除业务数据。
- 审批通过、驳回、撤回。
- 合同作废、终止、归档。
- 财务打款、发票作废。
- 库存出入库、调拨、盘点确认。
- 生产计划发布、任务关闭。

AI 可以：

- 生成草稿。
- 预填表单。
- 给出建议。
- 解释原因。

AI 不应直接：

- 绕过页面权限修改数据。
- 直接审批或付款。
- 直接删除业务记录。

### 7.4 权限先于检索和生成

所有 AI 功能必须遵守：

1. 先判断用户是否有业务对象访问权限。
2. 再取业务上下文。
3. 再调用模型。
4. 输出结果只能包含用户有权看到的数据。
5. 记录 AI 调用日志。

### 7.5 先业务页面，后独立 AI 页面

优先在用户已经工作的页面嵌入 AI：

- 审批详情页。
- 报销详情页。
- 合同详情页。
- 客户详情页。
- 项目详情页。
- 库存详情页。
- 生产计划详情页。
- 网盘文件详情页。

独立 AI 聊天适合作为全局入口，但不应成为唯一入口。

---

## 8. 分阶段实施计划

### 阶段 0：基础验证与环境整理

目标：确保后续开发可验证。

任务：

1. 恢复本地 Python/Django 运行环境。
2. 能执行 `python manage.py check`。
3. 能执行 `python manage.py test dtcall apps.ai -v 1`。
4. 梳理当前未提交改动，避免 AI 规划落地时覆盖已有工作。
5. 确认生产使用的模型供应商、API Base、密钥配置方式。

验收：

- Django check 通过。
- 核心 smoke tests 可运行。
- AI 模型配置验证功能可用。

### 阶段 1：P0 业务助手 MVP

目标：上线第一批可见价值高、风险可控的 AI 助手。

范围：

1. 审批与财务 AI 审核助手。
2. 合同 AI 审查助手。
3. 客户跟进与销售助手。
4. 企业知识库问答助手。

技术任务：

1. 定义统一业务 AI 输出协议。
2. 建立 AI 调用门面或收敛现有调用。
3. 为 P0 场景增加结构化 Prompt。
4. 将结果写入已有业务字段或 AI 任务记录。
5. 在页面中加入 AI 面板或按钮。
6. 增加用户反馈入口。

验收：

- 每个 P0 场景至少有一个业务页面可用。
- 所有 AI 结果有日志。
- 所有高风险动作需要人工确认。

### 阶段 2：生产、库存、项目与会议闭环

目标：将 AI 从内容生成扩展到运营预警和协作闭环。

范围：

1. 项目风险与周报助手。
2. 库存与生产预警助手。
3. 会议纪要到任务闭环。
4. 消息摘要与待办聚合。

技术任务：

1. 建立跨模块上下文构建器。
2. 给项目、库存、生产等场景定义指标口径。
3. 将 AI 建议转化为消息、审批、任务或工作流交互。
4. 增加定时执行能力，生成日报/周报/预警。

验收：

- 项目详情页能生成风险摘要和周报草稿。
- 库存页面能给出补货建议。
- 会议纪要能提取待办并跳转创建任务。

### 阶段 3：AI 工作流模板化与质量治理

目标：让 AI 能力从功能点变成可配置能力。

范围：

1. 工作流模板库。
2. AI 质量评估。
3. A/B 测试。
4. 成本与调用量报表。
5. 管理层经营分析助手。

技术任务：

1. 建立模板分类：审批、合同、财务、CRM、生产、库存、会议、知识库。
2. 支持模板复制、发布、授权。
3. 对关键 AI 场景收集反馈。
4. 按任务类型统计调用量、耗时、错误率、满意度。

验收：

- 管理员能创建或复制 AI 工作流模板。
- 用户反馈能进入统计。
- 可以看出哪个模型或提示词效果更好。

---

## 9. 推荐的第一批需求拆分

### 9.1 需求 1：统一 AI 结果卡片组件

用户故事：

作为业务用户，我希望在审批、合同、客户、项目等页面看到统一样式的 AI 建议卡片，以便快速理解 AI 的结论和依据。

功能：

- 展示摘要、风险等级、风险点、建议动作、置信度。
- 支持复制建议。
- 支持“采纳/不采纳”反馈。
- 支持重新生成。

### 9.2 需求 2：审批/报销详情页 AI 审核

用户故事：

作为审批人，我希望 AI 自动总结申请内容并提示异常风险，以便更快做出判断。

功能：

- 一键生成 AI 审核建议。
- 输出风险等级和原因。
- 一键填入审批意见。
- 保留用户最终审批动作。

### 9.3 需求 3：合同详情页 AI 审查

用户故事：

作为合同管理员，我希望 AI 提取合同关键条款并提示风险，以便快速完成初审。

功能：

- 合同摘要。
- 关键条款列表。
- 风险点。
- 履约提醒建议。
- 保存分析结果。

### 9.4 需求 4：客户详情页 AI 跟进助手

用户故事：

作为销售，我希望 AI 根据客户资料和跟进历史生成客户画像和下一步建议，以便提高跟进质量。

功能：

- 客户画像。
- 跟进摘要。
- 下一步计划。
- 跟进记录润色。
- 销售话术草稿。

### 9.5 需求 5：知识库问答增强

用户故事：

作为员工，我希望直接询问公司制度、操作流程和项目文档，并看到答案来源。

功能：

- RAG 问答。
- 来源条目展示。
- 无结果时明确提示。
- 权限过滤。

---

## 10. 验证与指标体系

### 10.1 功能验证

| 场景 | 验证方式 |
|---|---|
| 模型配置 | 验证默认模型可连接，错误时返回安全提示 |
| 审批 AI | 使用普通审批、异常审批、缺附件审批分别测试 |
| 合同 AI | 使用有付款条款、缺付款条款、超长合同分别测试 |
| 客户 AI | 使用新客户、老客户、高频跟进客户分别测试 |
| 知识库问答 | 使用命中、部分命中、无命中、无权限文档分别测试 |
| 权限 | 普通用户不能通过 AI 获取无权业务数据 |
| 日志 | 每次 AI 调用都有用户、模型、业务类型、状态记录 |

### 10.2 业务指标

| 指标 | 说明 |
|---|---|
| AI 建议采纳率 | 用户点击采纳或复制建议的比例 |
| 审批处理时间 | AI 上线前后平均处理时间变化 |
| 合同初审时间 | 合同风险分析节省的人工阅读时间 |
| 知识库命中率 | 用户问题能找到有效来源的比例 |
| 用户满意度 | AI 反馈评分 |
| 错误率 | 模型调用失败、解析失败、权限拒绝比例 |
| 成本 | 按模型、任务类型统计 token 和费用 |

---

## 11. 风险与治理建议

| 风险 | 建议 |
|---|---|
| AI 幻觉 | 输出必须包含依据或来源；无法确认时提示“不确定” |
| 数据越权 | 先权限过滤，再检索和生成 |
| 自动化误操作 | 高风险动作只生成草稿和建议，不直接执行 |
| 成本不可控 | 记录调用量、缓存分析结果、限制超长输入 |
| 多模型接口混乱 | 收敛到统一 AI 调用门面 |
| Prompt 难维护 | 按任务类型集中管理提示词和结构化输出 Schema |
| 结果不可评估 | 接入反馈、A/B 测试和场景级指标 |
| 本地环境不可验证 | 先恢复 Python/Django 环境，再进入功能实施 |

---

## 12. 建议优先级总表

| 优先级 | 功能 | 价值 | 复杂度 | 风险 | 推荐顺序 |
|---|---|---:|---:|---:|---:|
| P0 | 审批与财务 AI 审核助手 | 高 | 中 | 中 | 1 |
| P0 | 合同 AI 审查助手 | 高 | 中 | 中 | 2 |
| P0 | 客户跟进与销售助手 | 高 | 中 | 低 | 3 |
| P0 | 企业知识库问答助手 | 高 | 中 | 中 | 4 |
| P1 | 项目风险与周报助手 | 中高 | 中 | 中 | 5 |
| P1 | 库存与生产预警助手 | 中高 | 高 | 中 | 6 |
| P1 | 会议纪要到任务闭环 | 中 | 中 | 低 | 7 |
| P1 | 消息摘要与待办聚合 | 中 | 中 | 低 | 8 |
| P2 | 经营驾驶舱 AI 分析 | 高 | 高 | 中 | 9 |
| P2 | AI 工作流模板市场 | 中高 | 高 | 中 | 10 |
| P2 | AI 质量评估与 A/B 测试 | 中 | 中 | 低 | 11 |

---

## 13. 下一步建议

建议先做一次小范围 MVP：

1. 选定 2 个 P0 场景：审批/报销审核、合同审查。
2. 定义统一业务 AI 输出协议。
3. 统一调用入口和日志记录。
4. 在详情页接入 AI 结果卡片。
5. 加入采纳/不采纳反馈。
6. 用 10-20 条真实或测试数据验证结果质量。

完成 MVP 后，再扩展到客户跟进和知识库问答。这样既能快速展示业务价值，又不会把 AI 能力继续做散。

---

## 14. 本次分析依据

本规划基于以下当前项目材料和源码观察：

| 类型 | 位置 |
|---|---|
| 项目速览 | `documents/00_文档导航与项目速览.md` |
| PRD | `documents/03_PRD_产品需求文档.md` |
| 业务模块与数据模型 | `documents/04_业务模块与数据模型说明.md` |
| Django 配置 | `dtcall/settings.py`、`dtcall/urls.py` |
| AI 模型与工作流 | `apps/ai/models.py`、`apps/ai/urls.py` |
| AI 服务 | `apps/ai/services/`、`apps/ai/utils/` |
| 前端 AI 助手 | `static/js/ai-agent-sdk.js` |
| 业务 AI 入口 | `apps/*/ai_views.py` |
| 测试入口 | `dtcall/tests.py`、`apps/ai/tests.py` |

当前本地环境中 `python` 命令不可用，`py` 启动器无法找到可用 Python，Codex 自带 Python 未安装 Django，因此本次未能完成 Django 运行时检查。进入实施前应先恢复可运行环境。
